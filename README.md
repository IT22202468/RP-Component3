# Research 1.0 — Electron app

Minimal Electron app to run the Research 1.0 desktop project.

Quick start

1. Install dev dependencies (from project root):

   npm install

2. Start the app:

   npm run start

Notes

- This project uses a simple `index.js` main process and `index.html` renderer.
- For production you should review security settings (disable nodeIntegration, use preload, enable contextIsolation, etc.).
